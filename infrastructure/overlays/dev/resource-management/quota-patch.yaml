apiVersion: v1
kind: ResourceQuota
metadata:
  name: globomantics-quota
  namespace: globomantics-dev
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 2Gi
    limits.cpu: "2"
    limits.memory: 4Gi
    persistentvolumeclaims: "3"
    pods: "5"
    services: "3" 